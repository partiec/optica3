spring:
  application:
    name: optica3
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/opticadb
    username: rolek
    password: 123
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  flyway:
    enabled: false
  profiles:
    active: dev